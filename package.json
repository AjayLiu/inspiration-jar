{  
  "name": "inspirationjar",
  "version": "1.0.0",
  "description": "share words of encouragement with fellow humans around the world!",
  "main": "index.js",

  "scripts": {
    "start": "node index.js",
    "heroku-postbuild": "cd client && yarn && yarn build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "pg": "^8.5.1"
  }
}
